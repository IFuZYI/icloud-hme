# Git
.git
.gitignore

# 运行时数据与日志（由卷挂载提供，不进镜像）
data
logs

# 本地构建产物与二进制
icloud-hme
build
*.exe

# 前端依赖与构建产物（Dockerfile 内由 web-builder 阶段生成）
web/node_modules
internal/webui/dist

# 容器生态配置
.dockerignore
docker-compose.yml

# CI 配置
.github
