import { useEffect, useState } from 'react'
import { ApiError, request } from '../api/client'

type Log={id:string;task_id:string;time:string;level:string;message:string}
export default function LogsPage(){const [logs,setLogs]=useState<Log[]>([]),[error,setError]=useState('');useEffect(()=>{request<Log[]>('/api/alias-task-logs').then(setLogs).catch(e=>setError(e instanceof ApiError?e.message:'日志加载失败'))},[]);return <section><div className="page-header"><div className="page-title"><h2>任务日志</h2><p>查看自动创建任务的完整运行记录</p></div></div>{error&&<div className="alert-error">{error}</div>}<div className="table-wrap"><table><thead><tr><th>时间</th><th>任务</th><th>级别</th><th>内容</th></tr></thead><tbody>{logs.map(l=><tr key={l.id}><td>{new Date(l.time).toLocaleString()}</td><td>{l.task_id}</td><td><span className={l.level==='error'?'badge badge-error':'badge badge-active'}>{l.level}</span></td><td>{l.message}</td></tr>)}</tbody></table></div>{logs.length===0&&<p className="empty-state">暂无日志</p>}</section>}
